# ADR 0008: Рекомендации по использованию слотов в компонентах Svelte 5

**Статус:** Accepted  
**Дата:** 2025-11-05  
**Команда:** stylist-svelte

## Контекст

В ходе разработки компонентов библиотеки stylist-svelte были обнаружены предупреждения о deprecated использовании `<slot>` в Svelte 5. Согласно документации Svelte 5, в некоторых контекстах использование `<slot>` считается устаревшим, особенно когда компонент не является явно оберточным.

## Проблема

Компоненты Link и Label генерировали предупреждения при проверке типов:
```
Using `<slot>` to render parent content is deprecated. Use `{@render ...}` tags instead
```

## Решение

Принято решение использовать альтернативный подход для передачи содержимого в компоненты:

1. **Для простых текстовых компонентов**: испоользовать проп `text` для передачи строкового содержимого
2. **Для сложных компонентов**: использовать проп `children` типа `Snippet` для передачи компонентов, которые будут рендериться с помощью `{@render children()}`

### Пример реализации:

Вместо:
```svelte
<!-- Не рекомендуется в Svelte 5 -->
<script lang="ts">
  import type { HTMLAnchorAttributes } from 'svelte/elements';
  // ... другие импорты
</script>

<a>
  <slot />
</a>
```

Рекомендуется:
```svelte
<!-- Рекомендуется для Svelte 5 -->
<script lang="ts">
  import type { HTMLAnchorAttributes } from 'svelte/elements';
  import type { Snippet } from 'svelte';

  type Props = {
    text?: string;
    children?: Snippet;
  } & HTMLAnchorAttributes;

  let { text, children, ...restProps }: Props = $props();
</script>

<a {...restProps}>
  {#if children}
    {@render children()}
  {:else if text}
    {text}
  {/if}
</a>
```

### Когда использовать каждый подход:

- **`text` проп**: для простого текстового содержимого
- **`children` проп с `Snippet`**: для сложного содержимого, которое включает другие компоненты или HTML-разметку
- **`<slot>`**: все еще допустимо в случаях, когда компонент является очевидной "оберткой" и используется в традиционном подходе

## Последствия

### Положительные

- 🟢 Устранение предупреждений компилятора Svelte 5
- 🟢 Совместимость с современными практиками Svelte 5
- 🟢 Более явное API для передачи содержимого

### Отрицательные

- 🔴 Некоторое усложнение API компонента (необходимость выбирать между `text` и `children`)
- 🔴 Необходимость адаптации существующих компонентов при переходе на новый подход

## Решение реализовано в

- `src/lib/components/atoms/Link.svelte`: использование `text` и `children` вместо `<slot>`
- `src/lib/components/atoms/Label.svelte`: использование `text` и `children` вместо `<slot>`

## Связанные ADR

- ADR 0004 — архитектура компонентов
- ADR 0007 — ко-сопровождение playground stories рядом с компонентами