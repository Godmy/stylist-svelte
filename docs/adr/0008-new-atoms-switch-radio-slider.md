# ADR 0008: Расширение атомного уровня — Switch, RadioGroup, RangeSlider, Tag, Divider

**Статус:** Accepted  
**Дата:** 2025-11-04  
**Команда:** stylist-svelte

## Контекст

Библиотека «stylist-svelte» стартовала с базовых текстовых контролов (Button, Input, Checkbox и т.д.). Для реальных интерфейсов не хватало:

- компактных переключателей для настроек;
- управления выбором из набора вариантов;
- визуальных контролов для числовых значений;
- семантических бейджей/тегов;
- простого разделителя секций.

Из-за этого разработчики либо создавали временные решения, либо использовали сторонние компоненты, что ломало единый стиль и мешало развитию playground.

## Решение

Добавлены пять атомов с унифицированным API и историями:

1. **Switch.svelte** — бинарный переключатель (`sm | md | lg`, `checked`, `description`, `required`, `disabled`).
2. **RadioGroup.svelte** — группа радиокнопок с описанием опций, горизонтальной/вертикальной раскладкой, поддержкой ошибок и `bind:value`.
3. **RangeSlider.svelte** — одиночный слайдер с кастомным треком, отображением текущего значения и диапазона `min/max/step`.
4. **Tag.svelte** — интерактивный тег (варианты `neutral | primary | success | warning | danger`, размеры, иконка, closable-режим с событием `close`).
5. **Divider.svelte** — горизонтальный/вертикальный разделитель с подписью, выравниванием (`start | center | end`) и пунктирной линией.

Каждый компонент снабжён Story-файлом, что позволяет playground автоматически показывать применение и управлять контролами.

## Последствия

### Положительные

- 🟢 Формам и настройкам хватит встроенных атомов без сторонних зависимостей.
- 🟢 Playground стал богаче: появилась наглядная демонстрация переключателей, радиогрупп, слайдеров и декоративных элементов.
- 🟢 Вся реализация следует принятым стандартам (Svelte 5 runes, Tailwind, ARIA).

### Отрицательные

- 🔴 RangeSlider использует кастомную стилизацию thumb/trail — потребуется следить за совместимостью в разных браузерах.
- 🔴 В будущем стоит предусмотреть расширенные версии (диапазонный слайдер, многострочные теги, Divider с иконками) — занесено в бэклог.

## Обновления документации

- README перечисляет новые атомы и отмечает их наличие в playground.
- ADR 0007 по playground не требует правок: новые stories автоматически подхватываются по текущей схеме.

## Связанные ADR

- ADR 0004 — архитектура компонентов (Atomic Design).
- ADR 0007 — co-located stories для playground.
