# ADR 0007: Коу-сопровождение playground stories рядом с компонентами

**Статус:** Accepted  
**Дата:** 2025-11-04  
**Команда:** stylist-svelte

## Контекст

Изначальная версия playground (ADR 0003) предусматривала ручное добавление историй и не описывала конкретное расположение файлов. По мере роста библиотеки мы столкнулись с проблемами:

- истории приходилось подключать вручную, что замедляло развитие компонентов;
- отсутствовало единое соглашение, где хранить примеры, из-за чего новые участники тратили время на поиск;
- кодогенератор, контрольная панель и поиск давали пустые результаты, если история не указывала компонент и метаданные;
- документация (README и PLAYGROUND) содержала примеры с маршрутами в `src/routes`, что расходилось с фактической реализацией.

## Решение

Принято решение:

1. **Ко-локировать истории** рядом с исходными компонентами (`*.story.svelte` в `src/lib/components/**`).
2. **Автоматически регистрировать истории** через `import.meta.glob` в `src/routes/+page.svelte`, чтобы playground подхватывал новые примеры без модификации маршрутов.
3. **Требовать явного указания компонента и метаданных** (`component`, `tags`, `controls`) в историях для работы CodeViewer и поиска.
4. **Поддерживать сортировку и синхронизацию** в `playgroundStore`, чтобы истории появлялись по алфавиту, а значения контролов сохранялись при обновлении метаданных.
5. **Расширить покрытие**: создать истории для атомов и молекул, демонстрирующие реальные сценарии использования (поиск, формы, пустые состояния, работы с буфером обмена).
6. **Актуализировать документацию** (README, PLAYGROUND), заменив примеры с `src/routes/button/+page.svelte` на co-located stories.

## Последствия

### Положительные

- 🟢 Нулевые ручные правки при добавлении новых историй.
- 🟢 Playground сразу отображает актуальные компоненты и контролы.
- 🟢 Команде проще ориентироваться: пример находится рядом с компонентом.
- 🟢 Документация и реализация синхронизированы.
- 🟢 Улучшается опыт пользователей playground благодаря заполненному сайдбару и работающим контролам.

### Отрицательные

- 🔴 Build вытягивает все `*.story.svelte`, что слегка увеличивает время запуска dev-сервера.
- 🔴 Необходимо следить, чтобы истории не попадали в сборку пакета (`dist`) — решается текущей конфигурацией `svelte-package` (stories исключены).

## Решение реализовано в

- `src/routes/+page.svelte`: автоматическая подгрузка историй.
- `src/lib/playground/stores/playground.svelte.ts`: сортировка, автопереход и восстановление контролов.
- `src/lib/components/**.story.svelte`: набор историй для atoms и molecules.
- `README.md`, `PLAYGROUND.md`: обновлённые инструкции и примеры.

## Связанные ADR

- ADR 0003 — основание для playground
- ADR 0004 — архитектура компонентов
