# ADR 0009: Расширение уровня молекул

**Статус:** Accepted  
**Дата:** 2025-11-04  
**Команда:** stylist-svelte

## Контекст

После внедрения новых атомов (ADR 0008) в библиотеке всё ещё не хватало базовых композитных компонентов: автодополнения, множественного ввода тегов, удобной загрузки файлов, карточек метрик и готовой панели фильтров. В проектах команды эти паттерны появляются регулярно, а их «ручная» сборка в каждом продукте приводила к дублированию кода и разрозненному UX.

## Решение

Мы добавили шесть молекул, которые опираются на существующие атомы и сценарии из ADR 0003 (playground):

1. **Combobox** — поле с поиском и подсказками, поддерживает очистку, состояние загрузки и кастомное сообщение «ничего не найдено».
2. **TagInput** — управляет массивом тегов с подсказками и свободным вводом, учитывает ограничение по количеству и позволяет отключать компонент.
3. **FileUpload** — drag-and-drop область со списком файлов, валидацией размера и простыми статусами (`pending`, `success`, `error`).
4. **StatsCard** — компактная карточка метрик с трендом, иконкой и поясняющим текстом.
5. **FilterBar** — объединяет переключатели, теги и числовой слайдер в единую панель фильтрации.
6. **NotificationItem** — карточка уведомления с индикатором прочтения, списком тегов и слотами под действия.

Для каждого компонента создана story (`*.story.svelte`), чтобы playground автоматически демонстрировал API и сценарии.

## Последствия

### Положительные

- 🟢 Разработчики получают готовый набор паттернов, используемых в продуктовых фичах.
- 🟢 Playground покрывает ключевые сценарии: выбор, фильтрацию, уведомления, загрузку файлов.
- 🟢 Компоненты опираются на существующие атомы (Switch, Tag, RangeSlider) и держат единый визуальный язык.

### Отрицательные

- 🔴 Combobox и TagInput реализуют базовую функциональность; расширенная поддержка ARIA (например, roving tabindex) может понадобиться позже.
- 🔴 FileUpload не взаимодействует с сетью — интеграцию с API по-прежнему настраивает потребитель.

## Обновления документации

- README дополнен списком новых молекул.
- Индекс `src/lib/components/molecules/index.ts` экспортирует новые компоненты.
- Stories лежат рядом с компонентами, что соответствует ADR 0007.

## Связанные ADR

- ADR 0004 — архитектура компонентов (Atomic Design).
- ADR 0007 — playground и co-located stories.
- ADR 0008 — новые атомы, используемые внутри молекул.
