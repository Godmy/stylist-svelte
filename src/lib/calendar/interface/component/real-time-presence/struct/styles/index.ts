/**
 * RealTimePresence style classes interface
 */

export interface IRealTimePresenceStyleClasses {
  base: string;
  user: string;
  status: string;
  online: string;
  away: string;
  offline: string;
}
