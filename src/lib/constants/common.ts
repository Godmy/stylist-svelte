// Common constants for the entire library
export const DEFAULT_VARIANT = 'default';
export const DEFAULT_SIZE = 'md';
export const DEFAULT_CLOSEABLE = false;
export const DEFAULT_DISABLED = false;

// CSS class constants
export const BASE_CLASSES = 'inline-flex items-center';
export const SIZE_CLASSES = {
  sm: 'text-xs',
  md: 'text-sm', 
  lg: 'text-base'
} as const;