export class StyleManagerStatusIndicator {
	static root(classes: string): string {
		return classes.trim();
	}

	static container(classes: string): string {
		return classes.trim();
	}

	static dot(classes: string): string {
		return classes.trim();
	}

	static indicator(classes: string): string {
		return classes.trim();
	}

	static label(classes: string): string {
		return classes.trim();
	}
}
