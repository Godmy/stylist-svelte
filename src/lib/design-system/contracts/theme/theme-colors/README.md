# themes/contracts/theme-colors

Semantic color contract for theme objects.

## Purpose

- Define `ThemeColors` shape independent from raw color source scales.

## Change Policy

- Status: `Mostly closed`.
- Safe changes: add optional properties with defaults.
- Risky changes: rename/remove existing semantic keys.
