import type { AppearanceStyle, SemanticVariant } from '../tokens/variants';
import type { ComponentSize } from '../tokens/sizes';

/**
 * Алиасы размеров/вариантов, завязанных на дизайн-токены.
 */
export type DefaultSizes = ComponentSize;
export type DefaultVariants = SemanticVariant | AppearanceStyle;
