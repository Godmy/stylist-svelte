# themes/runtime/utils/css

Theme-to-CSS-variable conversion and DOM application helpers.

## Purpose

- Convert `Theme` data into CSS variable maps.
- Apply and remove variables on DOM safely.
- Resolve mode and scheme into effective DOM state.

## Change Policy

- Status: `Closed core + scalable aliases`.
- Keep existing variable names stable.
- New aliases are allowed if backward compatibility is required.
