# themes/theme/variants/typography

Shared typography fragment for theme variants.

## Purpose

- Compose font family, font size, font weight, and line height values.
- Use `Scale` for ordered weight steps and `Size` for dimension values.

