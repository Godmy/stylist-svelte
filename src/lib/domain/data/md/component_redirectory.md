# component_redirectory

Проверка вложенности компонентных директорий в `domain/component/{atom,molecule,organism}`.

## Правильный путь

- `<domain>/component/atom/<family>`
- `<domain>/component/atom/<family-trait>`
- `<domain>/component/atom/<family-trait-trait>`
- Аналогично для `molecule` и `organism`

## Нарушения

Нарушений не найдено.