import type { TierContent } from '$stylist/architecture/type/enum/tier';

export const DEFAULT_DRAGGABLE = {
	disabled: false,
	draggable: true,
	showHandle: true,
	showMenu: true,
	variant: 'default' as TierContent
};
