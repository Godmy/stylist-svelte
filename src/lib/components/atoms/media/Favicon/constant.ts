// Default fallback icon
export const DEFAULT_FALLBACK_ICON = '🌐';

// Default size for the favicon
export const DEFAULT_FAVICON_SIZE = 16;

// Name of the favicon file
export const FAVICON_FILE_NAME = 'favicon.ico';

// CSS classes for the image element
export const FAVICON_IMAGE_CLASSES = 'inline-block align-middle';

// CSS classes for the fallback element
export const FALLBACK_ICON_CLASSES = 'inline-flex items-center justify-center align-middle bg-[--color-bg-tertiary] text-[--color-text-tertiary] rounded-sm';