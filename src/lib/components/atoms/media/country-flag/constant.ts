// Default fallback flag emoji
export const DEFAULT_FALLBACK_FLAG = '🌍';

// Default size for the flag
export const DEFAULT_FLAG_SIZE = 24;

// Minimum length for a valid country code
export const MIN_COUNTRY_CODE_LENGTH = 2;

// Maximum length for a valid country code
export const MAX_COUNTRY_CODE_LENGTH = 2;

// Base code for country flag emoji calculation
export const COUNTRY_FLAG_BASE_CODE = 127397;

// CSS classes for the fallback element
export const FALLBACK_FLAG_CLASSES = 'inline-flex items-center justify-center align-middle text-[--color-text-tertiary] bg-[--color-bg-tertiary] rounded-sm';

// CSS classes for the flag element
export const FLAG_ELEMENT_CLASSES = 'inline-block text-center align-middle';