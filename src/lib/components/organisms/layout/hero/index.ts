/**
 * Точка входа для компонента Hero
 * 
 * Экспортирует основной компонент и все связанные типы
 */

import Hero from './Hero.svelte';
export { Hero };

export type { IHeroProps, HeroBackgroundVariant, HeroHeight, IHeroStat, IHeroCTA } from './types';